export const validateRegister = (email,password) => {
    if (email===""||password==="") {
        return alert('Rellena los campos requeridos');
      }
      alert(`Te registraste con el correo : ${email}`)
     }
    
